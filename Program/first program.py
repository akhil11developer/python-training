"""
accept some value from user 
by default input function accept value from string format

if we want to convert it into integer format

syntax:
varname = input(prompt message)


"""
name = int(input("enter your name:"))
print("name= ",name)